# config/redis/redis.conf
# Redis 기본 설정

# 네트워크
bind 0.0.0.0
port 6379
protected-mode no

# 메모리 관리
maxmemory 256mb
maxmemory-policy allkeys-lru

# 영속성 (AOF)
appendonly yes
appendfsync everysec

# 로깅
loglevel notice

# 클라이언트 연결
timeout 0
tcp-keepalive 300

# 성능 튜닝
tcp-backlog 511